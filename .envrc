# direnv: auto-enter the Nix dev shell on `cd` into the repo.
# One-time: `direnv allow`. Requires direnv + nix with flakes enabled.
# Falls back silently if nix/direnv aren't installed — the repo still
# builds with a system rustup + qemu as before.
use flake
